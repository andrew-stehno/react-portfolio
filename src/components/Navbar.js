import React from 'react';
import classes from './Navbar.module.css';

const navbar = () => {
    return (
        <h1 className={classes.NavigationText}>This will be my navigation menu!</h1>
    );
};

export default navbar;