import React from "react";
import classes from "./Footer.module.css"

const footer = () => {
  return (
    <div className={classes.Footer}>
      <p>My footer in your asser!</p>
    </div>
  );
};

export default footer;
